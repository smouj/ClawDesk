{
  "name": "clawdesk",
  "version": "2.0.0",
  "description": "Local Mission Control dashboard for OpenClaw",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "vitest run",
    "smoke": "node --test tests/smoke.test.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "tar": "^6.2.1"
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "globals": "^17.3.0",
    "prettier": "^3.3.3",
    "vitest": "^2.1.1"
  }
}
